# nixpacks.toml — Railway build config
# Instala Node.js + Python + edge-tts

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "pip install edge-tts --break-system-packages"
]

[start]
cmd = "node index.js"
